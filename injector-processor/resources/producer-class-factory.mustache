package {{packageName}};

import injector.*;

public class {{simpleName}}Factory implements Factory<{{simpleName}}> {

    @Override
    public {{simpleName}} create(Injector context, Class targetClass) {
        return context.instanceOf({{type}}.class, targetClass).{{#methods}}{{name}}(
        targetClass
{{#parameterIterable}}
{{^data.targetClass}},
{{^data.allOf}}context.instanceOf( {{data.type}}.class, targetClass ){{/data.allOf}}
{{#data.allOf}}context.instancesExposedAs( {{data.allOf}} ){{/data.allOf}}
{{/data.targetClass}}
{{/parameterIterable}}
    {{/methods}}
        );
    }

    @Override
    public Class<{{simpleName}}> getExposedType() {
        return {{simpleName}}.class;
    }
}
